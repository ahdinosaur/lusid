# Sample zshrc managed by lusid (examples/dotfiles).
# Replace with whatever your shell needs — edits here propagate immediately
# to `~/.zshrc` because `local apply` symlinks rather than copies.

export EDITOR=hx
export PAGER=less

alias ll='ls -lah'
